/**
 * This configures the navigation sidebar.
 *
 * - All entries MUST include `text` and `slug`
 * - Heading entries MUST include `header: true`
 */
 export default [
	{ name: 'API', header: true, slug: 'api' },
	{ name: 'Authentication', slug: 'api/authentication' },
	{ name: '/aliases', slug: 'api/aliases' },
 ]
